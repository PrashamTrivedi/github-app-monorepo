{
  "name": "github-app-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub App with Cloudflare Workers and Containers",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.3.0"
  }
}