{
  "name": "@github-app/backend",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub App backend API with Hono and Cloudflare Workers",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "echo 'Build handled by wrangler'",
    "deploy": "wrangler deploy",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "lint": "echo 'Linting not configured'",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@github-app/shared": "workspace:*",
    "hono": "^4.0.0",
    "@octokit/app": "^14.0.0",
    "@octokit/rest": "^20.0.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240129.0",
    "@types/node": "^22.0.0",
    "@types/jsonwebtoken": "^9.0.5",
    "typescript": "^5.3.0",
    "wrangler": "^4.33.0"
  }
}